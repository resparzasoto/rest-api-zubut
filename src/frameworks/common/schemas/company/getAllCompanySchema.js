'use strict';

const headersSchema = {};

const paramsSchema = {};

const querySchema = {};

const bodySchema = {};

const responseSchema = {};

module.exports = {
  headers: headersSchema,
  params: paramsSchema,
  query: querySchema,
  body: bodySchema,
  response: responseSchema,
};
